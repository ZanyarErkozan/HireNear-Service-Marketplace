# HireNear

Expo / React Native UI for a local service marketplace — find someone nearby, check rate, book a slot.

Mock data only (Turkish names, Istanbul-ish locations). No backend to set up.

## Run

```bash
npm install
npx expo start
```

Screenshots are under `assets/`.
